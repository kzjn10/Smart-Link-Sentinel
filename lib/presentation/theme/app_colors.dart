import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand Primary ──
  static const Color primary = Color(0xFF28feaf);
  static const Color primaryLight = Color(0xFF80FFD0);
  static const Color primaryDark = Color(0xFF00C27A);
  static const Color onPrimary = Color(0xFF003822);
  static const Color primaryContainer = Color(0xFFB2FFE4);
  static const Color onPrimaryContainer = Color(0xFF002114);

  // ── Action Accent (mapped to secondary) ──
  static const Color accent = Color(0xFF00E8FF);
  static const Color accentLight = Color(0xFF80F3FF);
  static const Color accentDark = Color(0xFF00B8CC);
  static const Color onAccent = Color(0xFF003640);
  static const Color accentContainer = Color(0xFFB3F7FF);
  static const Color onAccentContainer = Color(0xFF002028);

  // ── Surface ──
  static const Color surface = Color(0xFFF9F9F9);
  static const Color onSurface = Color(0xFF1A1C1E);
  static const Color surfaceDark = Color(0xFF121618);
  static const Color onSurfaceDark = Color(0xFFE2E2E6);

  // ── Status: High ──
  static const Color statusHigh = Color(0xFFE86F1B);
  static const Color statusHighLight = Color(0xFFFFA664);
  static const Color onStatusHigh = Color(0xFFFFFFFF);
  static const Color statusHighContainer = Color(0xFFFFDBC8);
  static const Color onStatusHighContainer = Color(0xFF4E2600);

  // ── Status: Done ──
  static const Color statusDone = Color(0xFF7EE787);
  static const Color statusDoneLight = Color(0xFFB4F1BA);
  static const Color onStatusDone = Color(0xFF003909);
  static const Color statusDoneContainer = Color(0xFFC8F5CC);
  static const Color onStatusDoneContainer = Color(0xFF002106);

  // ── Neutral / Outline ──
  static const Color outline = Color(0xFF72787E);
  static const Color outlineVariant = Color(0xFFC2C7CE);

  // ── Error ──
  static const Color error = Color(0xFFBA1A1A);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color onErrorContainer = Color(0xFF410002);
}
